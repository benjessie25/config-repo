# --- DEV profile overrides ---
server:
  port: ${DISCOVERY_PORT:8761}

eureka:
  instance:
    # En dev "tout docker", garde discovery-service.
    # Si tu lances le serveur sur l'hôte, exporte EUREKA_HOSTNAME=localhost.
    hostname: ${EUREKA_HOSTNAME:discovery-service}
