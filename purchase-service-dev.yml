# --- DEV profile overrides ---
server:
  port: 8070

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:15433/purchase_server
    username: dodash_pg
    password: Dodash#@2025

  kafka:
    producer:
      bootstrap-servers: localhost:19092
