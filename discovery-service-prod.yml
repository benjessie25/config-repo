# --- PROD profile overrides ---
server:
  port: ${EUREKA_PORT:8761}

eureka:
  instance:
    # Hostname configurable via ENV, fallback to container name
    hostname: ${EUREKA_HOSTNAME:discovery-service}
