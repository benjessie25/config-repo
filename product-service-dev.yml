# --- DEV profile overrides ---
server:
  port: 8050

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/product_server
    username: postgres
    password: postgres
